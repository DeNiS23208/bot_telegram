# 💾 Резервное копирование и безопасность

## 🚨 Критически важно!

База данных содержит всю информацию о:
- Пользователях
- Подписках и доступах
- Платежах
- Пригласительных ссылках

**Потеря базы данных = потеря всех данных о подписках и платежах!**

## ✅ Что уже настроено

### 1. Автоматическое резервное копирование каждые 2 часа
- Скрипт `backup_database.py` создает сжатую копию базы данных
- Отправляет на email `xasanimbuiss@mail.ru`
- Файлы не сохраняются на сервере (удаляются после отправки)
- База данных сжимается (обычно в 3-5 раз)

### 2. Ротация логов
- Логи хранятся 7 дней
- Автоматически удаляются старые логи

## 📋 Рекомендации по безопасности

### 1. **Множественные копии (3-2-1 правило)**

**3 копии данных:**
- ✅ Оригинал на сервере
- ✅ Резервные копии на email (каждые 2 часа)
- ⚠️ **РЕКОМЕНДУЕТСЯ:** Дополнительное копирование на облачное хранилище

**2 разных носителя:**
- ✅ Email (уже настроено)
- ⚠️ **РЕКОМЕНДУЕТСЯ:** Облачное хранилище (Google Drive, Yandex Disk, Dropbox)

**1 копия в другом месте:**
- ✅ Email хранится на серверах Mail.ru (внешнее хранилище)

### 2. **Дополнительные меры безопасности**

#### A. Облачное хранилище (рекомендуется)

Настройте автоматическую загрузку резервных копий в облако:

**Вариант 1: Google Drive**
```bash
# Установка rclone
apt install rclone
# Настройка подключения к Google Drive
# Автоматическая загрузка backup файлов
```

**Вариант 2: Yandex Disk**
```bash
# Установка Yandex Disk CLI
# Настройка синхронизации
```

#### B. Локальное хранилище (для критических данных)

Периодически скачивайте резервные копии с email на локальный компьютер или внешний диск.

#### C. Мониторинг

Настройте уведомления, если резервное копирование не удалось:
- Проверяйте email регулярно
- Настройте мониторинг логов

### 3. **Шифрование (опционально, но рекомендуется)**

Для дополнительной безопасности можно шифровать резервные копии перед отправкой:

```python
# Использование GPG для шифрования
# Только вы сможете расшифровать backup
```

### 4. **Проверка целостности**

Скрипт автоматически проверяет:
- ✅ Формат SQLite базы данных
- ✅ Целостность файла

### 5. **План восстановления**

**Если база данных потеряна:**

1. **Восстановление из email:**
   - Найдите последнюю резервную копию в почте
   - Скачайте файл `.db.gz`
   - Распакуйте: `gunzip bot_db_backup_YYYYMMDD_HHMMSS.db.gz`
   - Замените на сервере: `cp bot_db_backup_YYYYMMDD_HHMMSS.db /opt/bot_telegram/bot.db`
   - Перезапустите сервисы: `systemctl restart telegram-bot.service webhook.service`

2. **Проверка после восстановления:**
   - Проверьте количество пользователей
   - Проверьте активные подписки
   - Проверьте последние платежи

## 📊 Статистика резервного копирования

**Частота:** Каждые 2 часа (12 раз в день)
**Размер базы данных:** Обычно 50-500 KB
**Размер сжатого файла:** Обычно 15-150 KB
**Хранение на email:** Неограниченно (пока не удалите вручную)

## 🔒 Безопасность данных

### Что защищено:
- ✅ База данных сжимается (экономия места)
- ✅ Файлы не хранятся на сервере
- ✅ Резервные копии отправляются на защищенный email

### Что можно улучшить:
- ⚠️ Шифрование резервных копий (опционально)
- ⚠️ Дополнительное копирование в облако
- ⚠️ Мониторинг успешности копирования

## 📧 Проверка резервных копий

**Регулярно проверяйте:**
1. Приходят ли письма с резервными копиями каждые 2 часа
2. Размер файлов (должен быть примерно одинаковым)
3. Дата последней копии

**Если копии не приходят:**
- Проверьте логи: `tail -f /opt/bot_telegram/logs/backup_database.log`
- Проверьте настройки SMTP в `.env`
- Проверьте cron задачу: `crontab -l`

## 🛡️ Дополнительные меры защиты сервера

1. **Регулярные обновления:**
   ```bash
   apt update && apt upgrade -y
   ```

2. **Мониторинг дискового пространства:**
   ```bash
   df -h
   ```

3. **Мониторинг использования ресурсов:**
   ```bash
   htop
   ```

4. **Резервное копирование конфигурации:**
   - Сохраните `.env` файл в безопасном месте
   - Документируйте настройки сервера

## ⚠️ Важные замечания

1. **Email ограничения:**
   - Mail.ru может ограничивать размер вложений (обычно до 25 MB)
   - База данных обычно меньше 1 MB, проблем не будет
   - Если база вырастет > 10 MB, рассмотрите облачное хранилище

2. **Хранение на email:**
   - Email хранит все письма, включая старые копии
   - Периодически очищайте старые копии (оставляйте последние 30 дней)
   - Или настройте автоудаление старых писем

3. **Тестирование восстановления:**
   - Раз в месяц тестируйте восстановление из backup
   - Убедитесь, что процесс работает

## 📞 Что делать в случае потери данных

1. **Немедленно:**
   - Остановите сервисы (чтобы не перезаписать данные)
   - Найдите последнюю резервную копию в email
   - Восстановите базу данных

2. **После восстановления:**
   - Проверьте целостность данных
   - Проверьте активные подписки
   - Уведомите пользователей, если необходимо

3. **Анализ:**
   - Выясните причину потери данных
   - Улучшите систему резервного копирования
   - Добавьте дополнительные меры защиты

