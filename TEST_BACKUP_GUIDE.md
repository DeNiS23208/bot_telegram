# 🧪 Руководство по тестированию резервного копирования

## Зачем тестировать?

Тестирование резервного копирования критически важно, потому что:
- ✅ Убеждает, что backup создается правильно
- ✅ Проверяет, что восстановление работает
- ✅ Дает уверенность в случае реальной проблемы
- ✅ Выявляет проблемы до того, как они станут критическими

## 🧪 Вариант 1: Автоматическое тестирование (рекомендуется)

### На сервере выполните:

```bash
cd /opt/bot_telegram
chmod +x test_backup_restore.sh
./test_backup_restore.sh
```

Скрипт автоматически:
1. ✅ Проверит текущую базу данных
2. ✅ Создаст резервную копию
3. ✅ Проверит целостность backup
4. ✅ Протестирует восстановление
5. ✅ Восстановит оригинальную базу
6. ✅ Проверит работу сервисов

**Безопасно:** Скрипт создает копию оригинальной базы перед тестом и восстанавливает её после.

## 🧪 Вариант 2: Ручное тестирование (пошагово)

### Шаг 1: Проверка текущей базы данных

```bash
cd /opt/bot_telegram

# Проверяем размер и содержимое
ls -lh bot.db
sqlite3 bot.db "SELECT COUNT(*) as users FROM users;"
sqlite3 bot.db "SELECT COUNT(*) as payments FROM payments;"
sqlite3 bot.db "SELECT COUNT(*) as subscriptions FROM subscriptions;"
```

**Запишите эти данные** - они понадобятся для проверки!

### Шаг 2: Создание тестовой резервной копии

```bash
# Создаем backup вручную
cp bot.db test_backup_$(date +%Y%m%d_%H%M%S).db

# Или используем скрипт резервного копирования
source venv/bin/activate
python3 backup_database.py
```

**Проверьте email** - должно прийти письмо с backup файлом!

### Шаг 3: Проверка backup файла

```bash
# Если backup на сервере
sqlite3 test_backup_*.db "SELECT COUNT(*) FROM users;"
sqlite3 test_backup_*.db "SELECT COUNT(*) FROM payments;"

# Если backup скачан с email
gunzip bot_db_backup_*.db.gz
sqlite3 bot_db_backup_*.db "SELECT COUNT(*) FROM users;"
```

**Сравните** с данными из Шага 1 - должно совпадать!

### Шаг 4: Тестирование восстановления

⚠️ **ВАЖНО:** Перед тестом создайте копию оригинальной базы!

```bash
# Сохраняем оригинальную базу
cp bot.db bot_original_backup.db

# Останавливаем сервисы
systemctl stop telegram-bot.service webhook.service

# Восстанавливаем из backup
cp test_backup_*.db bot.db
# Или если backup с email:
# gunzip bot_db_backup_*.db.gz
# cp bot_db_backup_*.db bot.db

# Проверяем восстановленную базу
sqlite3 bot.db "SELECT COUNT(*) FROM users;"
sqlite3 bot.db "SELECT COUNT(*) FROM payments;"
sqlite3 bot.db "SELECT COUNT(*) FROM subscriptions;"

# Запускаем сервисы
systemctl start telegram-bot.service webhook.service

# Проверяем статус
systemctl status telegram-bot.service webhook.service
```

### Шаг 5: Восстановление оригинальной базы

```bash
# Если все работает, можно оставить восстановленную базу
# Или вернуть оригинальную:
cp bot_original_backup.db bot.db
systemctl restart telegram-bot.service webhook.service
```

## 📧 Тестирование отправки на email

### Проверка отправки backup на email:

```bash
# Запускаем скрипт резервного копирования
cd /opt/bot_telegram
source venv/bin/activate
python3 backup_database.py
```

**Проверьте:**
1. ✅ Письмо пришло на `xasanimbuiss@mail.ru`
2. ✅ В письме есть вложение `.db.gz`
3. ✅ Размер файла разумный (обычно 15-150 KB)
4. ✅ Файл можно скачать

### Проверка отправки Excel отчета:

```bash
python3 send_excel_report.py
```

**Проверьте:**
1. ✅ Письмо пришло с Excel файлом
2. ✅ Файл можно открыть
3. ✅ Данные в файле актуальные

## 🔍 Проверка логов

### Логи резервного копирования:

```bash
tail -f /opt/bot_telegram/logs/backup_database.log
```

**Что искать:**
- ✅ "✅ Резервная копия создана"
- ✅ "✅ Резервная копия валидна"
- ✅ "✅ Резервная копия успешно отправлена"
- ❌ Ошибки (если есть)

### Логи отправки отчетов:

```bash
tail -f /opt/bot_telegram/logs/email_report.log
```

## 📅 Регулярное тестирование

**Рекомендуется тестировать:**

1. **Еженедельно:**
   - Проверяйте, что письма приходят каждые 2 часа
   - Проверяйте размер backup файлов (должен быть примерно одинаковым)

2. **Ежемесячно:**
   - Полное тестирование восстановления (используйте скрипт)
   - Проверка целостности данных после восстановления

3. **При изменениях:**
   - После обновления сервера
   - После изменения структуры базы данных
   - После проблем с сервером

## ⚠️ Что делать если тест не прошел

### Backup не создается:
1. Проверьте права доступа к базе данных
2. Проверьте наличие свободного места на диске
3. Проверьте логи: `tail -50 /opt/bot_telegram/logs/backup_database.log`

### Backup не отправляется:
1. Проверьте настройки SMTP в `.env`
2. Проверьте пароль приложения Mail.ru
3. Проверьте логи на ошибки

### Восстановление не работает:
1. Проверьте, что backup файл не поврежден
2. Проверьте права доступа к файлу
3. Убедитесь, что сервисы остановлены перед восстановлением

## ✅ Чек-лист успешного теста

- [ ] Backup создается без ошибок
- [ ] Backup файл валиден (можно открыть в SQLite)
- [ ] Количество записей в backup совпадает с оригиналом
- [ ] Backup отправляется на email
- [ ] Восстановление из backup работает
- [ ] После восстановления сервисы работают
- [ ] Данные после восстановления совпадают с оригиналом

## 💡 Дополнительные советы

1. **Храните несколько backup:**
   - Последний backup может быть поврежден
   - Храните backup за последние 3-7 дней

2. **Тестируйте на тестовой базе:**
   - Создайте тестовую базу данных
   - Протестируйте восстановление на ней
   - Не рискуйте с рабочей базой

3. **Документируйте процесс:**
   - Записывайте результаты тестов
   - Отмечайте даты тестирования
   - Фиксируйте проблемы и решения

