#!/bin/bash
# Скрипт для автоматического коммита и пуша изменений в репозиторий

set -e

REPO_DIR="/Users/gdm/Documents/bot_telegram"
cd "$REPO_DIR"

# Проверка наличия изменений
if [ -z "$(git status --porcelain)" ]; then
    echo "✅ Нет изменений для коммита"
    exit 0
fi

# Добавление всех изменений
echo "📦 Добавление изменений..."
git add -A

# Создание коммита с текущей датой и временем
COMMIT_MSG="Update: $(date '+%Y-%m-%d %H:%M:%S')"
echo "💾 Создание коммита: $COMMIT_MSG"
git commit -m "$COMMIT_MSG"

# Пуш в репозиторий
echo "🚀 Отправка изменений в репозиторий..."
git push origin main

echo "✅ Изменения успешно отправлены в репозиторий"

