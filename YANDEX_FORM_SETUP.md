# Инструкция по настройке Яндекс.Формы для интеграции с ботом

## Обзор

Эта инструкция поможет настроить Яндекс.Форму для автоматической отправки данных на наш webhook при заполнении формы пользователем.

## Шаг 1: Открыть настройки интеграции

1. Откройте вашу форму: https://forms.yandex.ru/u/69592c7e068ff04fd8f00241/
2. Перейдите на вкладку **"Интеграции"** (в верхней части страницы)
3. Вы увидите раздел **"Настройка интеграций"**

## Шаг 2: Добавить действие API

1. В разделе **"Группа действий #1"** (или создайте новую группу, нажав **"+ Добавить группу действий"**)
2. Нажмите кнопку **"API"**
3. В выпадающем меню выберите **"Запрос JSON-RPC POST"** (это важно!)

## Шаг 3: Настроить POST запрос

### 3.1. URL ⚠️ ВАЖНО - ЗАПОЛНИТЕ ЭТО ПОЛЕ!

В поле **"URL?"** (самое первое поле вверху) введите **ТОЧНО** этот URL:

```
https://xasanim.ru/yandex-form/webhook?token={{url.token}}
```

**📝 Скопируйте и вставьте это значение:**
```
https://xasanim.ru/yandex-form/webhook?token={{url.token}}
```

**Важно:** 
- Используйте именно этот URL (с доменом xasanim.ru)
- Обязательно должен быть `https://` (не `http://`)
- Путь должен быть точно `/yandex-form/webhook`
- **ОБЯЗАТЕЛЬНО добавьте `?token={{url.token}}` в конец URL** - это извлечет токен из URL формы (`?token=...`) и передаст его в URL webhook запроса
- `{{url.token}}` - это переменная Яндекс.Формы, которая извлекает параметр `token` из URL формы

### 3.1.1. Метод (для JSON-RPC POST)

В поле **"Метод"** должно быть написано:
```
POST
```

**✅ Проверьте:** Если там уже написано `POST` - оставьте как есть. Если пусто - введите `POST`.

### 3.2. Метод ⚠️ ВАЖНО - ИЗМЕНИТЕ НА JSON-RPC POST!

**❌ ПРОБЛЕМА:** Обычный POST не работает с `{{answers}}` - Яндекс.Формы не может правильно сформировать JSON.

**✅ РЕШЕНИЕ:** Используйте JSON-RPC POST!

В поле **"Метод"** выберите:
```
Запрос JSON-RPC POST
```

**📝 Важно:** 
- НЕ используйте обычный "POST" или "Запрос заданным методом"
- Выберите именно "Запрос JSON-RPC POST" из списка методов
- Это специальный формат, который Яндекс.Формы поддерживает лучше

### 3.3. Параметры ⚠️ ВАЖНО - ДЛЯ JSON-RPC POST!

**✅ При использовании JSON-RPC POST нужно добавить параметр:**

1. Нажмите кнопку **"+ Добавить параметр"** (справа от поля "Параметры")

2. В появившемся поле введите:
   - **Ключ (имя параметра):** `answers`
   - **Значение:** `{{answers}}`

**📝 Важно:**
- Ключ должен быть точно `answers` (без кавычек)
- Значение должно быть точно `{{answers}}` (без кавычек и фигурных скобок)
- `{{answers}}` - это переменная Яндекс.Формы, которая автоматически заменится на все ответы пользователя из формы
- Бот автоматически распарсит ответы и найдет пользователя по email

**✅ РЕЗУЛЬТАТ:** Бот автоматически найдет пользователя по email из ответов формы. Убедитесь, что в форме есть поле для ввода email, и оно называется `email` или `почта`.

### 3.4. Заголовки ✅ (Вы уже настроили правильно!)

Судя по вашему скриншоту, заголовок уже настроен правильно!

1. **Заголовок 1:**
   - Ключ: `Content-Type`
   - Значение: `application/json`
   - ✅ У вас правильно!

**📝 Важно:** Не меняйте этот заголовок, он уже настроен правильно!

### 3.5. Сообщение о результате ✅ (Вы уже настроили правильно!)

Судя по вашему скриншоту, вы уже выбрали **"Не показывать"** - это правильно!

**📝 Важно:** Оставьте как есть - "Не показывать" выделено синим цветом.

## Шаг 4: Настроить передачу токена через URL формы

**✅ ХОРОШИЕ НОВОСТИ:** В боте мы уже настроили автоматическую передачу токена в URL формы!

Когда пользователь нажимает кнопку "Заполнить данные" в боте, он автоматически получает ссылку вида:
```
https://forms.yandex.ru/u/69592c7e068ff04fd8f00241/?token=abc123xyz...
```

Где `abc123xyz...` - это уникальный токен пользователя.

**📝 Что нужно проверить:**
1. Убедитесь, что форма принимает параметр `token` в URL (обычно Яндекс.Формы это поддерживают автоматически)
2. В параметре `params` вы уже указали `{"token": "{{token}}"}` - это правильно, Яндекс.Формы автоматически подставит токен из URL

**⚠️ Если токен не передается:**
Можно попробовать альтернативный вариант - передать токен через URL webhook:
```
https://xasanim.ru/yandex-form/webhook?token={{token}}
```

Но сначала попробуйте текущую настройку - она должна работать!

## Шаг 5: Альтернативный вариант (если JSON-RPC не работает)

Если JSON-RPC POST не работает, можно использовать обычный HTTP POST запрос:

1. Вместо **"Запрос JSON-RPC POST"** выберите **"Запрос заданным методом"**
2. Метод: `POST`
3. URL: `https://ваш-домен.com/yandex-form/webhook?token={{token}}`
4. В теле запроса (Body) добавьте JSON:
```json
{
  "token": "{{token}}",
  "answers": {
    "name": "{{name}}",
    "phone": "{{phone}}",
    "email": "{{email}}",
    "gender": "{{gender}}",
    "city": "{{city}}",
    "activity": "{{activity}}"
  }
}
```

## Шаг 6: Сохранить настройки ⚠️ ВАЖНО!

1. **Сначала заполните поле URL:**
   - В самом первом поле **"URL?"** введите: `https://xasanim.ru/yandex-form/webhook`
   
2. **Проверьте все параметры** (они уже настроены правильно, просто убедитесь):
   - ✅ jsonrpc: 2.0
   - ✅ method: form.submitted
   - ✅ id: 1
   - ✅ params: {"token": "{{token}}"}
   - ✅ params.answers: {{answers}}

3. **Проверьте заголовок:**
   - ✅ Content-Type: application/json

4. **Проверьте переключатели:**
   - ✅ Переключатель действия должен быть **ВКЛЮЧЕН** (синий, справа вверху)
   - ✅ Переключатель группы действий должен быть **ВКЛЮЧЕН** (синий)

5. **Нажмите кнопку "Сохранить"** (синяя кнопка внизу)

**📝 После сохранения:** Форма будет автоматически отправлять данные на ваш webhook при каждом заполнении!

## Шаг 7: Тестирование

1. Откройте форму в режиме просмотра
2. Заполните форму тестовыми данными
3. Отправьте форму
4. Проверьте логи на сервере - должен прийти webhook с данными
5. Проверьте, что пользователь получил уведомление в боте: "✅ Отлично! Данные получены"

## Структура данных, которые отправляет форма

Яндекс.Формы могут отправлять данные в разных форматах. Наш webhook обрабатывает следующие варианты:

### Вариант 1: JSON-RPC
```json
{
  "jsonrpc": "2.0",
  "method": "form.submitted",
  "params": {
    "token": "user_token_here",
    "answers": {
      "name": "Иван",
      "phone": "+79991234567",
      "email": "ivan@example.com",
      "gender": "Мужской",
      "city": "Москва",
      "activity": "Программирование"
    }
  },
  "id": 1
}
```

### Вариант 2: Обычный JSON
```json
{
  "token": "user_token_here",
  "answers": {
    "name": "Иван",
    "phone": "+79991234567",
    "email": "ivan@example.com",
    "gender": "Мужской",
    "city": "Москва",
    "activity": "Программирование"
  }
}
```

## Важные замечания

1. **Токен обязателен:** Без токена бот не сможет идентифицировать пользователя
2. **Проверка домена:** Убедитесь, что ваш домен доступен из интернета
3. **HTTPS обязателен:** Яндекс.Формы требуют HTTPS для webhook
4. **Тестирование:** Всегда тестируйте на реальном сервере, не на localhost

## Устранение неполадок

### Webhook не приходит

1. Проверьте, что URL правильный и доступен
2. Проверьте логи сервера: `journalctl -u webhook -f`
3. Проверьте, что переключатель действия включен в настройках формы
4. Проверьте, что форма опубликована

### Токен не передается

1. Убедитесь, что токен добавлен в URL формы в боте
2. Проверьте, что параметр `token` правильно настроен в интеграции
3. Проверьте логи - там должно быть видно, какой токен пришел

### Данные формы не сохраняются

1. Проверьте логи webhook - там видно, какие данные пришли
2. Проверьте структуру данных - возможно, названия полей отличаются
3. Проверьте базу данных - таблица `form_data` должна создаться автоматически

## Контакты для поддержки

Если возникли проблемы, проверьте:
- Логи сервера: `journalctl -u webhook -f`
- Логи бота: `journalctl -u telegram-bot -f`
- База данных: `sqlite3 /opt/bot_telegram/bot.db "SELECT * FROM users WHERE form_filled = 1;"`

