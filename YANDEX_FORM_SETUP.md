# Инструкция по настройке Яндекс.Формы для интеграции с ботом

## Обзор

Эта инструкция поможет настроить Яндекс.Форму для автоматической отправки данных на наш webhook при заполнении формы пользователем.

**✅ НОВАЯ ЛОГИКА:** Теперь вместо токена используется Telegram ID пользователя, что делает интеграцию более надежной.

## Шаг 1: Открыть настройки интеграции

1. Откройте вашу форму: https://forms.yandex.ru/u/69592c7e068ff04fd8f00241/
2. Перейдите на вкладку **"Интеграции"** (в верхней части страницы)
3. Вы увидите раздел **"Настройка интеграций"**

## Шаг 2: Добавить действие API

1. В разделе **"Группа действий #1"** (или создайте новую группу, нажав **"+ Добавить группу действий"**)
2. Нажмите кнопку **"API"**
3. В выпадающем меню выберите **"Запрос JSON-RPC POST"** (это важно!)

## Шаг 3: Настроить POST запрос

### 3.1. URL ⚠️ ВАЖНО - ЗАПОЛНИТЕ ЭТО ПОЛЕ!

В поле **"URL?"** (самое первое поле вверху) введите **ТОЧНО** этот URL:

```
https://xasanim.ru/yandex-form/webhook?token={{url.token}}
```

**📝 Скопируйте и вставьте это значение:**
```
https://xasanim.ru/yandex-form/webhook?token={{url.token}}
```

**Важно:** 
- Используйте именно этот URL (с доменом xasanim.ru)
- Обязательно должен быть `https://` (не `http://`)
- Путь должен быть точно `/yandex-form/webhook`
- Добавьте `?token={{url.token}}` в конец URL - это передаст токен из URL формы
- Если `{{url.token}}` не подставляется - не страшно, бот использует fallback

### 3.2. Метод ⚠️ ВАЖНО - ИЗМЕНИТЕ НА JSON-RPC POST!

**✅ РЕШЕНИЕ:** Используйте JSON-RPC POST!

В поле **"Метод"** выберите:
```
Запрос JSON-RPC POST
```

**📝 Важно:** 
- НЕ используйте обычный "POST" или "Запрос заданным методом"
- Выберите именно "Запрос JSON-RPC POST" из списка методов
- Это специальный формат, который Яндекс.Формы поддерживают лучше

### 3.3. Параметры ⚠️ ВАЖНО - ДЛЯ JSON-RPC POST!

**✅ При использовании JSON-RPC POST можно добавить параметр token (опционально):**

1. Нажмите кнопку **"+ Добавить параметр"** (справа от поля "Параметры")

2. В появившемся поле введите:
   - **Ключ (имя параметра):** `token`
   - **Значение:** `{{url.token}}`

**⚠️ ВАЖНО:** Яндекс.Формы могут не подставлять переменные `{{url.token}}` или `{{token}}` - они могут остаться как шаблоны.

**✅ РЕШЕНИЕ:** Бот использует fallback механизм - если токен не найден, он автоматически найдет последнего пользователя, который недавно нажал кнопку "Заполнить данные" (в течение последних 30 минут).

**📝 Важно:**
- Параметр token опционален - можно не добавлять
- Если переменные не подставляются (остаются как `{{url.token}}`), это нормально - бот использует fallback
- **Главное:** Нажмите кнопку "Заполнить данные" в боте и сразу заполните форму (в течение 30 минут) - тогда fallback сработает

**✅ РЕЗУЛЬТАТ:** Даже если переменные не подставляются, бот найдет пользователя через fallback механизм и отправит уведомление о заполнении формы.

### 3.4. Заголовки ✅

1. **Заголовок 1:**
   - Ключ: `Content-Type`
   - Значение: `application/json`

**📝 Важно:** Этот заголовок обязателен для JSON-RPC запросов!

### 3.5. Сообщение о результате ✅

Выберите **"Не показывать"** - это правильно!

**📝 Важно:** Оставьте как есть - "Не показывать" выделено синим цветом.

## Шаг 4: Настроить передачу Telegram ID через URL формы

**✅ ХОРОШИЕ НОВОСТИ:** В боте мы уже настроили автоматическую передачу Telegram ID в URL формы!

Когда пользователь нажимает кнопку "Заполнить данные" в боте, он автоматически получает ссылку вида:
```
https://forms.yandex.ru/u/69592c7e068ff04fd8f00241/?telegram_id=123456789
```

Где `123456789` - это Telegram ID пользователя.

**📝 Что нужно проверить:**
1. Убедитесь, что форма принимает параметр `telegram_id` в URL (обычно Яндекс.Формы это поддерживают автоматически)
2. В параметре `params` вы указали `{"telegram_id": "{{url.telegram_id}}"}` - это правильно, Яндекс.Формы автоматически подставит Telegram ID из URL

## Шаг 5: Сохранить настройки ⚠️ ВАЖНО!

1. **Сначала заполните поле URL:**
   - В самом первом поле **"URL?"** введите: `https://xasanim.ru/yandex-form/webhook?telegram_id={{url.telegram_id}}`
   
2. **Проверьте все параметры:**
   - ✅ jsonrpc: 2.0
   - ✅ method: form.submitted
   - ✅ id: 1
   - ✅ params: {"telegram_id": "{{url.telegram_id}}"}
   - ✅ params.answers: {{answers}} (опционально, если нужно передавать данные формы)

3. **Проверьте заголовок:**
   - ✅ Content-Type: application/json

4. **Проверьте переключатели:**
   - ✅ Переключатель действия должен быть **ВКЛЮЧЕН** (синий, справа вверху)
   - ✅ Переключатель группы действий должен быть **ВКЛЮЧЕН** (синий)

5. **Нажмите кнопку "Сохранить"** (синяя кнопка внизу)

**📝 После сохранения:** Форма будет автоматически отправлять данные на ваш webhook при каждом заполнении!

## Шаг 6: Тестирование

1. Откройте форму в режиме просмотра
2. Заполните форму тестовыми данными
3. Отправьте форму
4. Проверьте логи на сервере - должен прийти webhook с Telegram ID
5. Проверьте, что пользователь получил уведомление в боте: "✅ Отлично! Данные получены"

## Структура данных, которые отправляет форма

Яндекс.Формы могут отправлять данные в разных форматах. Наш webhook обрабатывает следующие варианты:

### Вариант 1: JSON-RPC с telegram_id в URL
```
URL: https://xasanim.ru/yandex-form/webhook?telegram_id=123456789
```

```json
{
  "jsonrpc": "2.0",
  "method": "form.submitted",
  "params": {
    "telegram_id": "123456789",
    "answers": {
      "name": "Иван",
      "phone": "+79991234567",
      "email": "ivan@example.com"
    }
  },
  "id": 1
}
```

### Вариант 2: JSON-RPC с telegram_id в params
```json
{
  "jsonrpc": "2.0",
  "method": "form.submitted",
  "params": {
    "telegram_id": "123456789"
  },
  "id": 1
}
```

### Вариант 3: Обычный JSON
```json
{
  "telegram_id": "123456789",
  "answers": {
    "name": "Иван",
    "phone": "+79991234567",
    "email": "ivan@example.com"
  }
}
```

## Важные замечания

1. **Telegram ID обязателен:** Без Telegram ID бот не сможет идентифицировать пользователя
2. **Проверка домена:** Убедитесь, что ваш домен доступен из интернета
3. **HTTPS обязателен:** Яндекс.Формы требуют HTTPS для webhook
4. **Тестирование:** Всегда тестируйте на реальном сервере, не на localhost
5. **Простота:** Теперь не нужно искать пользователя по email или использовать fallback механизмы - Telegram ID передается напрямую

## Устранение неполадок

### Webhook не приходит

1. Проверьте, что URL правильный и доступен
2. Проверьте логи сервера: `journalctl -u webhook -f`
3. Проверьте, что переключатель действия включен в настройках формы
4. Проверьте, что форма опубликована

### Telegram ID не передается

1. Убедитесь, что Telegram ID добавлен в URL формы в боте
2. Проверьте, что параметр `telegram_id` правильно настроен в интеграции
3. Проверьте логи - там должно быть видно, какой Telegram ID пришел
4. Убедитесь, что в URL формы используется `?telegram_id=...` (не `?token=...`)

### Данные формы не сохраняются

1. Проверьте логи webhook - там видно, какие данные пришли
2. Проверьте структуру данных - возможно, названия полей отличаются
3. Проверьте базу данных - таблица `form_data` должна создаться автоматически

### Пользователь не найден

1. Убедитесь, что пользователь сначала запустил бота командой `/start`
2. Проверьте, что Telegram ID в URL формы совпадает с реальным ID пользователя
3. Проверьте логи - там должно быть видно, какой Telegram ID пришел и найден ли пользователь

## Контакты для поддержки

Если возникли проблемы, проверьте:
- Логи сервера: `journalctl -u webhook -f`
- Логи бота: `journalctl -u telegram-bot -f`
- База данных: `sqlite3 /opt/bot_telegram/bot.db "SELECT * FROM users WHERE form_filled = 1;"`
