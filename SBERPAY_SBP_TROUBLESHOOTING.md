# Диагностика проблем с SberPay и СБП

## Проблема

Платежи через SberPay и СБП не проходят, webhook'и не приходят. Работает только оплата банковской картой.

## Что было исправлено

### 1. Добавлено детальное логирование

Теперь в логах бота отображается:
- Тип платежного метода из webhook notification
- Тип платежного метода из API ЮKassa
- Полная информация о payment_method объекте
- Статус обработки платежа

### 2. Унифицирована обработка всех типов платежей

- Подписка активируется для **всех типов платежей** (SberPay, СБП, карта)
- Активация происходит независимо от наличия или типа `payment_method`
- Добавлено логирование типа платежа при активации подписки

### 3. Добавлена поддержка возвратов

- Функция `create_refund()` работает для всех типов платежей
- Автоматическая обработка webhook'ов `refund.succeeded`

## Диагностика

### Шаг 1: Проверка логов

Проверьте логи бота при попытке оплаты через SberPay или СБП:

```bash
# На сервере
tail -f /opt/bot_telegram/logs/bot.log | grep -E "(SberPay|СБП|payment_method|webhook)"
```

Ищите в логах:
- `🔍 [WEBHOOK] Тип платежного метода:` - тип из webhook
- `🔍 [API] Тип платежного метода:` - тип из API
- `✅ Подписка активирована для пользователя` - активация подписки

### Шаг 2: Проверка webhook'ов

Убедитесь, что webhook'и приходят от ЮKassa:

```bash
# Проверка последних webhook'ов
grep "📥 Получено событие от ЮKassa" /opt/bot_telegram/logs/bot.log | tail -20
```

Если webhook'и не приходят:
1. Проверьте настройки webhook URL в личном кабинете ЮKassa
2. Убедитесь, что сервер доступен из интернета
3. Проверьте firewall и настройки безопасности

### Шаг 3: Проверка статуса платежа в ЮKassa

1. Зайдите в личный кабинет ЮKassa
2. Найдите платеж по payment_id
3. Проверьте:
   - Статус платежа
   - Тип платежного метода
   - Причину отмены (если платеж отменен)

### Шаг 4: Проверка настроек магазина

В личном кабинете ЮKassa проверьте:
1. **Подключенные способы оплаты**:
   - SberPay должен быть подключен
   - СБП должен быть подключен
   - Банковские карты должны быть подключены

2. **Настройки webhook'ов**:
   - URL webhook должен быть правильным
   - Webhook должен быть активен
   - Должны быть включены события: `payment.succeeded`, `payment.canceled`, `refund.succeeded`

3. **Лимиты и ограничения**:
   - Проверьте, нет ли ограничений на сумму платежа
   - Проверьте, нет ли ограничений на тип платежа

## Возможные причины проблем

### 1. Webhook'и не приходят

**Причины:**
- Неправильный URL webhook в настройках ЮKassa
- Сервер недоступен из интернета
- Firewall блокирует входящие соединения
- SSL сертификат недействителен

**Решение:**
- Проверьте доступность webhook URL: `curl https://your-domain.com/yookassa/webhook`
- Проверьте настройки firewall
- Проверьте SSL сертификат

### 2. Платежи не проходят

**Причины:**
- SberPay или СБП не подключены в настройках магазина
- Ограничения на сумму платежа
- Проблемы на стороне ЮKassa или банка

**Решение:**
- Обратитесь в поддержку ЮKassa: ecommerce@yoomoney.ru
- Проверьте настройки магазина в личном кабинете
- Попробуйте создать тестовый платеж через другой способ оплаты

### 3. Платежи проходят, но webhook'и не обрабатываются

**Причины:**
- Ошибка в обработке webhook'ов
- Проблемы с базой данных
- Ошибки в коде обработки

**Решение:**
- Проверьте логи бота на наличие ошибок
- Проверьте, что база данных доступна
- Проверьте, что все зависимости установлены

## Тестирование

### Тест 1: Проверка обработки разных типов платежей

1. Создайте тестовый платеж через банковскую карту
2. Проверьте логи - должна быть активация подписки
3. Создайте тестовый платеж через SberPay
4. Проверьте логи - должна быть активация подписки
5. Создайте тестовый платеж через СБП
6. Проверьте логи - должна быть активация подписки

### Тест 2: Проверка возвратов

1. Создайте успешный платеж
2. Создайте возврат через скрипт `create_refund.py`
3. Проверьте, что подписка отменена
4. Проверьте, что пользователь получил уведомление

## Контакты поддержки

- **ЮKassa поддержка**: ecommerce@yoomoney.ru
- **Техническая документация**: https://yookassa.ru/developers/api
- **Документация по webhook'ам**: https://yookassa.ru/developers/using-api/webhooks

## Дополнительная информация

### Типы платежных методов в ЮKassa

- `bank_card` - Банковская карта
- `sberpay` - SberPay
- `sbp` - СБП (Система быстрых платежей)
- `yoo_money` - ЮMoney
- `qiwi` - QIWI
- `cash` - Наличные
- `mobile_balance` - Мобильный баланс

### Логирование типов платежей

В логах бота теперь отображается:
```
🔍 [WEBHOOK] Тип платежного метода: sberpay, payment_method_id: xxx
🔍 [API] Тип платежного метода: sberpay, payment_method_id: xxx
✅ Подписка активирована для пользователя 123456 на 5 минут (тип платежа: sberpay)
```

Это поможет быстро определить, какой тип платежа используется и почему он не обрабатывается.

